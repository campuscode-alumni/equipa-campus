<%= flash[:notice]%>
<%= simple_form_for [@contract, @receipt] do |f| %>
  <%= f.input :name, label: 'Nome' %>
  <%= f.input :cpf, label: 'CPF'%>
  <%= f.submit 'Emitir recibo de devolução', class:'btn btn-primary'%>
<% end %>
